# 计算器项目依赖

# 核心依赖
python>=3.8

# GUI依赖
# 注意：tkinter通常随Python一起安装，但可能需要系统包
# Ubuntu/Debian: sudo apt-get install python3-tk

# 测试依赖
pytest>=7.0.0
pytest-cov>=4.0.0

# 开发工具
black>=22.0.0
flake8>=5.0.0
mypy>=0.991

# 可选：GUI测试工具
pyautogui>=0.9.0  # 用于自动化GUI测试